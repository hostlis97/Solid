public class Purchase {
    protected String title;
    protected int count;
//Был убран пустой не используемый конструктор
    public Purchase(String title, int count) {
        this.title = title;
        this.count = count;
    }
}

